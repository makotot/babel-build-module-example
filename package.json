{
  "name": "babel-cjs",
  "version": "1.0.0",
  "main": "index.js",
  "license": "MIT",
  "scripts": {
    "build:cjs": "BABEL_ENV=cjs babel src -d lib/cjs",
    "build:esm": "babel src -d lib/esm",
    "build": "yarn run build:cjs && yarn run build:esm"
  },
  "devDependencies": {
    "@babel/cli": "^7.8.4",
    "@babel/core": "^7.9.0",
    "@babel/preset-env": "^7.9.0"
  }
}
